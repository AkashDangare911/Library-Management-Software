export enum BorrowStatus {
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    ISSUED = 'issued',
    OVERDUE = 'overdue',
    RETURNED = 'returned',
    REJECTED = 'rejected',
    REVOKED = 'revoked',
    CANCELLED = 'cancelled'
}
